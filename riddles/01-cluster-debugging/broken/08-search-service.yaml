apiVersion: apps/v1
kind: Deployment
metadata:
  name: search-service
  namespace: riddle-1
  labels:
    app: search-service
    tier: backend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: search-service
  template:
    metadata:
      labels:
        app: search-service
        tier: backend
    spec:
      containers:
        - name: search
          image: nginx:alpine
          ports:
            - containerPort: 8080
          command: ["sh", "-c"]
          args:
            - |
              echo "$(date): Starting search-service..."
              echo "$(date): Loading product catalog..."
              echo "$(date): Search index ready."
              cat > /etc/nginx/conf.d/default.conf << 'CONF'
              server {
                  listen 8080;
                  location / { return 200 'search-service: OK\n'; }
                  location /health { return 200 'healthy\n'; }
              }
              CONF
              nginx -g 'daemon off;'
          resources:
            requests:
              cpu: "100m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "128Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8081
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: search-service
  namespace: riddle-1
spec:
  selector:
    app: search-service
  ports:
    - port: 8080
      targetPort: 8080
